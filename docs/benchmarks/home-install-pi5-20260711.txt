# geist Home Assistant Phase-1 staging check — Raspberry Pi 5
# Date: 2026-07-11 Europe/Berlin
# Host: rspdevelop, aarch64, Raspberry Pi OS / Linux 6.18.33
# Branch: feat/home-eval-extend
# Safety: DESTDIR staging only; live HA config, unit, process and socket unchanged.

Command shape:
  scripts/install-home-assistant.sh \
    --ha-config /home/germar/ha-config \
    --binary /home/germar/geist-public/bin/pi5/release/tools/geist-home \
    --env-file /home/germar/geist-public/geist-home.env \
    --user germar \
    --work-dir /home/germar/geist-public \
    --state-dir /home/germar/.local/share/geist-home \
    --unit-dir /etc/systemd/system \
    --destdir /tmp/geist-phase1-install

Installer wall time:
  real 0.02 s
  user 0.01 s
  sys  0.00 s

Staged artifact checks:
  PASS component contains the explicit 7-file allowlist
  PASS environment file mode 0600
  PASS generated systemd unit
  PASS model-free registry and transport contract

Read-only live health check after staging:
  PASS Unix socket exists and is mode 0600
  PASS systemd service active (PID 7880)
  PASS restart count 0
  PASS exposed registry synchronized within 600 s (39 devices in journal)
  PASS environment file mode 0600
  PASS authenticated Home Assistant API reachable

This is not the roadmap's <=10-minute clean-host measurement: build/model
download, initial HA configuration and first Assist action were intentionally
excluded. It proves the installer is fast and non-invasive on the target
hardware; a fresh image remains the acceptance environment for that gate.
