# geist disposable live-Home-Assistant acceptance — Raspberry Pi 5
# Date: 2026-07-11 Europe/Berlin
# Branch: feat/home-eval-extend
# Commit source under test: 8f65379 plus the live-test artifact in this commit
# Host: rspdevelop, aarch64, Raspberry Pi 5

Environment:
  Home Assistant: ghcr.io/home-assistant/home-assistant:stable (local image)
  HA endpoint: isolated container on 127.0.0.1:18123
  geist-home: freshly built from the feature worktree with embedded BitNet
  productive HA endpoint, systemd service, socket and config: unchanged

Fixture:
  light.flur    present in HA and in the geist registry
  light.keller  present in HA but absent from the geist registry

Requests and verified HA states:
  "Schalte das Licht im Flur ein"
    route: control_device
    observation: OK: light.flur -> an
    verified: light.flur=on, light.keller=off

  "Schalte das Licht im Keller ein"
    route: control_device
    observation: no matching registry device
    verified: light.keller=off

Result:
  PASS exposed=on hidden=off
  initial acceptance wall time: 30 s, including fresh HA startup and API onboarding

Resident-daemon warm simple-command latency:
  transport: one Unix-socket request -> model route -> authenticated HA action
             -> final daemon answer
  warm-up: one status request, excluded
  samples: 10 alternating light.flur on/off actions

  p50     2088 ms
  p95     2106 ms
  minimum 1962 ms
  maximum 2106 ms

  PASS roadmap warm simple-command p50 target <= 3 s

Extended run wall time:
  51 s, including fresh HA startup, onboarding, initial boundary action,
  daemon startup, warm-up, ten measured actions and hidden-entity denial

The test uses the official onboarding and token APIs, authenticates every REST
request, and invokes HA's real service endpoint. It proves the registry boundary
against an entity that genuinely exists in HA; it does not merely test an
unknown entity or an in-process state stub.
