build/
lib/
bin/
.DS_Store
__pycache__/
*.py[cod]

# Models are large (GBs) — fetched on demand via `make fetch-model`.
gguf_artifacts/
*.gguf
*.safetensors
*.part

# Leftover CLI symlinks from before the engine/agent split (the agent layer
# owns the binary now). This repo no longer produces them, but older working
# copies still have them lying around -- and one got committed that way.
/geist
/geist-bitnet

# Compiled example binaries.
examples/simple_generate
examples/push_to_talk

# Memory-palace test/selftest scratch dirs (created by the memory tools + tests).
.agent_memory_unit/
.mind_selftest/

# Benchmark run artifacts and scratch outputs. Runs now default to
# ~/bench-geistlib (outside the repo); bench_runs/ stays listed so older
# working copies keep ignoring their leftovers.
bench_runs/
*.npy
summary.json
module_tree.txt
tokens_ref.txt

# JetBrains IDE
.idea/

# internal agent config / scratch (not part of the public docs)
.agent/
.agent_memory_unit/
geist-home

# generated by tools/gen_test_wav.py (CI audio smoke)
audio_test_data/smoke.wav

# in-place built example binaries
/examples/dictate
/examples/simple_generate
/examples/push_to_talk
/examples/embed_smoke
/examples/agent_contract_smoke
/examples/geist_calibrate
