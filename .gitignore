build/
lib/
bin/
# `make` symlinks ./geist -> bin/<target>/<mode>/tools/geist
/geist
.DS_Store
__pycache__/
*.py[cod]

# Models are large (GBs) — fetched on demand via `make fetch-model`.
gguf_artifacts/
*.gguf
*.safetensors
*.part

# Compiled example binaries.
examples/simple_generate

# Memory-palace test/selftest scratch dirs (created by the memory tools + tests).
.agent_memory_unit/
.mind_selftest/

# Benchmark run artifacts and scratch outputs.
bench_runs/
*.npy
summary.json
module_tree.txt
tokens_ref.txt

# JetBrains IDE
.idea/
