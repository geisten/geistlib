{
 "note": "Runs produced by `make bench` and nothing else. Every entry carries the protocol that produced it, because a throughput number without one cannot be compared to anything -- that is what went wrong with headline_results.json, whose rows record no seq_len and whose Pi 'decode 17.4' turned out to be a short-context figure while the same build reads 12.8 at 512 tokens. The baseline is measured only after the board has returned to the temperature geist started from; without that the ratio measures the running order as much as the engines. Rows are appended by `make bench BENCH_ARGS=--record`; a plain `make bench` never writes here, so a stranger reproducing the numbers leaves no diff behind. Throughput is hardware-bound and therefore context, not comparison; the ratio column is the part that carries across machines.",
 "schema": {
  "date": "UTC date of the run",
  "system": "CPU as the machine names itself",
  "cores": "logical cores visible to the process",
  "ram_gb": "total RAM",
  "os": "kernel name and release",
  "model": "GGUF filename",
  "commit": "geist commit the binary was built from",
  "protocol": "frozen sweep parameters -- rows with differing protocols are not comparable",
  "decode_tps_512": "decode throughput at a 512-token prompt, mean of the repeats",
  "prefill_tps_512": "prefill throughput at a 512-token prompt",
  "spread_pct": "run-to-run spread as +/-% around the mean. Above ~2% the run varied; read it together with load before blaming contention -- on an idle board it is usually thermal drift over a long sweep",
  "baseline_engine": "competing engine measured in the same run, if its binary was reachable",
  "baseline_decode_tps": "its decode throughput under the same protocol",
  "decode_tps_short": "decode throughput at the shortest prompt -- the row that matches a baseline's empty-context generation benchmark",
  "decode_ratio_short_ctx": "geist / baseline decode, both from a near-empty context",
  "prefill_ratio_512": "geist / baseline prompt processing at 512 tokens",
  "baseline_prefill_tps": "baseline prompt-processing throughput at 512 tokens",
  "temp_c_geist_start": "board temperature when geist started",
  "temp_c_baseline_start": "board temperature when the baseline started; -1 means the run predates this field being recorded",
  "thermal_gate": "cooled = both engines verified at or below the gate; still_hot = the board would not come down in time and the ratios flatter geist; unreadable = no unprivileged thermometer, so the running order cannot be ruled out"
 },
 "runs": [
  {
   "date": "2026-08-01",
   "system": "Raspberry Pi 5 Model B Rev 1.1",
   "cores": 4,
   "ram_gb": 4.0,
   "os": "Linux 6.18.33+rpt-rpi-2712",
   "model": "bitnet-2b4t-i2_s.gguf",
   "commit": "9b24b1f",
   "protocol": {
    "seq_lens": "32,128,512",
    "decode_n": 64,
    "warmup": 64,
    "repeats": 10,
    "agg": "mean"
   },
   "decode_tps_512": 14.971,
   "prefill_tps_512": 45.462,
   "spread_pct": 1.1384004892622925,
   "baseline_engine": "bitnet.cpp",
   "baseline_decode_tps": 9.14,
   "decode_tps_short": 17.881,
   "decode_ratio_short_ctx": 1.9563457330415754,
   "prefill_ratio_512": 0.9926200873362446,
   "baseline_prefill_tps": 45.8,
   "temp_c_geist_start": 53.8,
   "temp_c_baseline_start": -1.0,
   "thermal_gate": "cooled"
  }
 ]
}
